
#  Check if a string is a palindrome.

str = input("Enter any string to check for palindrome\n")

reversed_str=''
for i in range(len(str), 0, -1):
    reversed_str+=str[i-1]
    
if str.lower() == reversed_str.lower():
    print(f"Given string {str} is palindrome")
else:
    print(f"Given string {str} is not palindrome")


# Output:

# Enter any string to check for palindrome
# Madam
# Given string Madam is palindrome *)
